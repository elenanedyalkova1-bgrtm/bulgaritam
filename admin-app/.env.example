# Existing Baserow variables remain required while Baserow is the live pipeline.
BASEROW_ANALYTICS_EVENTS_TABLE_ID=
BASEROW_API_TOKEN=

# Server-only Supabase analytics foundation. Never prefix the service key with PUBLIC_.
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
